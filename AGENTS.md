# Repository workflow

- After completing and validating requested code changes, commit the relevant files and push the current branch to `origin` without requiring the user to request it separately.
- Include only files related to the current request. Never stage unrelated user changes.
- If pushing is blocked by authentication, conflicts, failed checks, or an unclear change scope, report the blocker instead of forcing the operation.
